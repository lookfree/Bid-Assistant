# 送审材料里混着**我们自己**生成的辅助信息：识别文字的来源注记、图片占位、截断提示（统一以
# 「【系统注记」开头，见 parsing/types.SYSTEM_NOTE_PREFIX），以及 chapters 的键（sec-1 / t3 这类
# 章节编号，模型要靠它填 target_id，删不掉）。2026-08-11 生产实测（康恒环境）模型把它们当成了
# 投标文件本身的毛病：「[中风险] 投标文件多处出现章节编号(如 sec-xxx)和内嵌图片标记，未作清理，
# 影响文件整洁性和专业性」——用户的 .docx 里没有任何这类东西，既冤枉用户，也泄露内部实现。
# 这一段是提示词侧的防线（让模型看懂它们是什么、以及识别文字为什么可信度更低）；
# 数据侧的那一半在 schemas.RiskReport._derive_counts（确定性丢弃谈论注记/内部编号的发现）。
_SYSTEM_NOTE_RULE = """
【材料里的系统注记与章节编号（本条优先于一切「文件不整洁 / 有多余标记 / 未作清理」的判断）】
用户消息里有两类文字是**本系统生成的辅助信息，不是投标人写的内容**：
1. 以「【系统注记」开头的段落，标注紧随其后那段文字的来源或处理情况——
   「【系统注记·图片识别 第N张】」「【系统注记·扫描页识别 第N页】」：其后的文字是本系统从该图/该页
   识别出来的，**可能有识别误差，可信度低于可复制正文**，不得当成投标人的正式承诺逐字比对，
   更不得拿其中的错字判风险；「【系统注记·图片…】」：原文此处是一张图；
   「【系统注记·截断】」：这一章太长没能全部喂给你，后面还有内容，不等于文件写到这里为止。
2. chapters 的键（sec-1、t3 这类章节编号）：供你填 target_id 的定位编号。
这两类都**不属于投标文件的内容**：不得据此判定任何风险（尤其不得报「文件里有多余编号/标记、
未作清理、不整洁、不专业」），也不得抄进 title / advice / tender_ref / chapter_title / anchor_text。
"""

REVIEW_SYSTEM_PROMPT = """你是投标合规审查专家（废标体检）。把投标文件与招标要求逐条比对，给出风险体检报告。

输入：读标结论（含废标红线/★不可偏离/评分点）、提纲、各章正文，均在用户消息中。
检查要点：
1. ★不可偏离 / 强制资格：缺失或未响应 → 高风险（tone=destructive，多为废标项），写明对应招标条款与整改建议。
2. 实质性要求未明确承诺（如分级 SLA、服务期、保证金）→ 中风险（tone=warning）。
3. 业绩/资质举证不足、价格构成缺失 → 中风险。
4. 查重：章节间是否大段重复/套话堆砌 → 中风险提示。
5. 程序性格式红线（读标 format 分类）：招标要求编入投标文件的表单（偏离表/报价一览/声明承诺）在提纲与正文中
   缺章 → 高风险；份数/装订/密封封套书写/签字盖章/递交方式等操作性要求 → 若正文未体现响应，出一条中风险
   汇总提醒（逐项列出待人工核对的操作要求，勿逐条刷屏）。
6. 若用户消息给出 required_structure（投标文件构成清单）：其中 required=true 且 kind≠rule 的构成项，
   若提纲中无章节通过 structure_ref 或标题近似匹配响应 → 高风险（缺投标文件构成项，形式审查不过），
   写明缺失的构成项名称与对应招标条款。
7. 若提纲中存在偏离表类章节（章节标题含「偏离」，或 structure_ref 对应的构成项标题含「偏离」，
   如技术偏离表/商务偏离表）：读标标记为 ★ 的条目，若该偏离表章节正文找不到与之对应（按条款内容
   比对）的表格行 → 高风险（★条款漏登偏离表，形式审查不过），写明缺失的★条款内容与对应招标条款。
8. 已满足项归入 passed_items。
【渲染恒定项】导出时将恒定附加:封面、目录、投标人承诺与签章页、AI 生成说明页——招标构成要求命中上述项时判「已具备(导出恒定附加)」,不判缺失。
对每条风险给 chapter_title、tender_ref（"对应：…"）、advice、target_tab(tech/business)、target_id(章id)、
anchor_text（章内定位锚点：从该章正文原样摘抄 10–30 字，用于把用户带到出问题的那一处；
"缺少某内容"这类问题摘抄应当补写位置的邻近原文；实在无可摘抄给空字符串 ""，但**不可省略本字段**）。
字段取值必须严格合规：level 只能取「高风险」或「中风险」；tone 只能取 destructive（高风险）或 warning（中风险）；
target_tab 只能取 tech 或 business；score 为 0–100 整数。
给体检分 score；high/mid/passed 计数由系统按 items 自动推导，不必填。
你必须调用 submit_risk_report 工具一次性提交结果——直接输出文字不算完成任务；若提交被拒绝（校验错误），修正字段后重新提交。
忠于招标原文，不放过废标红线，也不虚构风险。
""" + _SYSTEM_NOTE_RULE

# 受审文件含扫描图片页时**追加**到系统提示（无扫描页时一个字都不加，提示词与此前逐字节一致）。
# 2026-08-09 生产实测：366 页的投标文件有 139 页是扫描件（身份证、法人授权书、盖章报价表），
# 这些页提不出任何文字，模型便把**实际存在**的材料判成「缺少」——19 条高风险大半是这种假阳性。
# 诚实分级：看不见就说「无法核验」，把核对交回给人，而不是替用户断言人家漏了材料。
SCAN_REVIEW_RULE = """
【看不见的内容怎么判（本次必须遵守，优先于上面第 1、5、6、7 条一切「缺失/未响应即高风险」的写法）】
受审的投标文件里有扫描图片页、或正文里贴着图片，那些页/那些图的内容你完全看不到。
凡是在给你的文字里找不到、但可能印在扫描页或图片上的材料（构成项、证照、资质、业绩证明、
盖章表格、签字页、身份证明等）：
1. 一律不得判成「缺少/未响应」的高风险，必须降为中风险（level=中风险，tone=warning）；
2. title 以「无法核验（扫描件）：」开头，例如「无法核验（扫描件）：法定代表人身份证明」；
3. advice 写明该材料可能已在扫描页或图片中、本次无法读取，请人工核对具体是哪一项/哪一份并确认；
4. 只有当看不见的页/图极少、明显装不下该材料时（例如全书只有一两页扫描件、只有一两张图），
   才允许判「缺少」的高风险。
在正文文字里确有内容、只是内容本身不合要求的，照常按原规则判定，不受本条影响。
"""


def _visibility_line(f: dict) -> str:
    """一份文件的可见性说明。扫描 PDF 报页数，docx 报正文内嵌图片张数——对模型是同一件事：
    这些内容你看不到，别据此断言人家缺材料。docx 没有「页」的口径，只能报张数。"""
    unseen = "其内容对你不可见——很可能包含证照、身份证明、盖章表格、签字页。"
    if f.get("image_pages"):
        return f"- 《{f['name']}》共 {f['pages']} 页，其中 {f['image_pages']} 页为扫描图片页，{unseen}"
    return f"- 《{f['name']}》正文中含 {f.get('embedded_images', 0)} 张内嵌图片，{unseen}"


def scan_pages_note(scanned: list[dict]) -> str:
    """受审文件的可见性说明（拼在用户消息最前面）。没有看不见的内容 → 空串，消息与此前逐字节一致。
    只讲人话，不出现任何内部字段名——模型要知道的是「有多少看不见、里面可能是什么」。"""
    if not scanned:
        return ""
    return "【受审文件可见性说明】\n" + "\n".join(_visibility_line(f) for f in scanned) + "\n"
